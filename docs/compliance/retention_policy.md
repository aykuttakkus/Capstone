# Retention Policy

This demo keeps raw chat data only for the configured raw chat retention period.

Session summaries and screening data follow the demo retention rules and are removed after the configured window.
