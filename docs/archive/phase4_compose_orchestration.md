# Phase 4 Compose Orchestration

There is no hidden startup-time coupling between services.

The backend and frontend are independent services under one compose project.

Use `docker compose up --build` for the full stack, or start each service separately when debugging.
