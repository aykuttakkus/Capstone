# Demo Package

```mermaid
flowchart TD
  A[Session restore] --> B[Evidence gate]
  B --> C[Privacy]
  C --> D[Answer]
```

- Session restore keeps the conversation context usable after refresh.
- Evidence gate prevents unsupported answers.
- Privacy rules keep the demo scoped and safe.
