[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: fast, isolated unit and service tests
    integration: API, database, and retrieval integration tests
    eval: evaluation contract and benchmark-adjacent tests
    slow: intentionally slower tests
