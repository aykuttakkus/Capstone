# =============================================================================
# Calma — Development & Testing Dependencies
# Install alongside requirements.txt for local development:
#   pip install -r requirements.txt -r requirements-dev.txt
# =============================================================================

-r requirements.txt

# Testing
pytest==9.0.2
pytest-asyncio==0.24.0
httpx==0.28.1   # already in prod but listed here for explicitness in test context
